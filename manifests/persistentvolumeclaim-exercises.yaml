apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "shared-data"
  namespace: "exercises"
spec:
  storageClassName: "local-storage-exercises"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
