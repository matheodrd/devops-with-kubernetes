apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "shared-data"
spec:
  storageClassName: "local-storage"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
